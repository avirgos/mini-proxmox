# mini-proxmox

## Prérequis

Paquets `php` et `sshpass` requis.

Debian :

```sh
sudo apt-get update
sudo apt-get install php sshpass -y
```

RHEL/CentOS :

```sh
sudo dnf update
sudo dnf install php sshpass -y
```

## Utilisation

```bash
./deploy.sh
```
