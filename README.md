# mini-proxmox

## Prérequis

Paquets `php` et `sshpass` requis.

Debian :

```
sudo apt-get update
sudo apt-get install php sshpass -y
```

RHEL/CentOS :

```
sudo dnf update
sudo dnf install php sshpass -y
```
